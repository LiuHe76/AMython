package component;

/**
 *   Each node in the Abstract Syntax Tree will be the instance of one of the Interface AST's implementation class.
 *   Abstract Syntax Tree is constructed along with the parsing process in order for later evaluation.
 */
public interface AST {
}
